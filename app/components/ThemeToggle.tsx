"use client";
import { useEffect,useState } from "react"; import {Moon,Sun} from "lucide-react";
export default function ThemeToggle(){const [dark,setDark]=useState(true);useEffect(()=>{const saved=localStorage.getItem('aura-theme');if(saved==='light'){setDark(false);document.documentElement.dataset.theme='light'}},[]);function toggle(){const next=!dark;setDark(next);document.documentElement.dataset.theme=next?'dark':'light';localStorage.setItem('aura-theme',next?'dark':'light')}return <button className="themeToggle" onClick={toggle} aria-label="Toggle theme">{dark?<Moon size={17}/>:<Sun size={17}/>}</button>}
