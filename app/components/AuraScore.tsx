"use client";
import {useEffect,useState} from 'react'; import {Sparkles,Trophy} from 'lucide-react'; import {createClient} from '@/lib/supabase/client';
export default function AuraScore(){const s=createClient();const [score,setScore]=useState(0);useEffect(()=>{async function load(){const {data:{user}}=await s.auth.getUser();if(!user)return;const [{count:p},{count:l},{count:c},{count:f}]=await Promise.all([s.from('posts').select('id',{count:'exact',head:true}).eq('author_id',user.id),s.from('post_likes').select('id',{count:'exact',head:true}).eq('user_id',user.id),s.from('comments').select('id',{count:'exact',head:true}).eq('user_id',user.id),s.from('follows').select('id',{count:'exact',head:true}).eq('follower_id',user.id)]);setScore(Math.min(9999,(p||0)*20+(l||0)*5+(c||0)*10+(f||0)*15))}load()},[]);return <div className="scoreCard panel"><div className="scoreIcon"><Sparkles size={22}/></div><div><div className="eyebrow">AURA SCORE</div><strong>{score}</strong><p>Keep creating, connecting and contributing.</p></div><Trophy size={22}/></div>}
